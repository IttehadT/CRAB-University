// src/config/db.config.ts

export const dbConfig = {
  useTier1_CDN: true,
  useTier2_SupabaseJSON: true, // We will build this later
  useTier3_MySQL: true,        // We will build this later
  useTier4_VectorAI: true,     // We will build this later
};